'use client'
import React, { ReactNode } from 'react';

export default function AdminPage ({ params: { lng } }: {
    params: {
      lng: string;
    };
  })  {
    
  return (
    <section>
    AdminPage
    </section>
  );
}
